spring:
  datasource:
    hikari:
      maximum-pool-size: '1'
    username: postgres
    url: jdbc:postgresql://localhost:5432/algafood_test
    password: '121797'
  flyway:
    locations: classpath:db/migration
